pb_data/
server
summareader-sync-server
summareader-sync
# The names the binary had before the rename.
allreader-sync-server

# Where scripts/build.sh puts things.
dist/

# The console's build output and pub cache. The Go module never sees console/
# — it has no .go files in it — but git would happily commit a 60 MB bundle.
console/build/
console/.dart_tool/
console/linux/flutter/ephemeral/
console/windows/flutter/ephemeral/
console/macos/Flutter/ephemeral/

# A real config, beside a real database. The example is the one that is
# committed.
summareader-sync.json
out/
