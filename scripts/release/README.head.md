# SummaReader sync server

A server that keeps a SummaReader library in step across your own devices. It
stores ciphertext against sequence numbers and holds no keys, so it cannot read
a word of what it carries.
